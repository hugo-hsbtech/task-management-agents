---
phase: 03-work-item-orchestrator-and-single-cycle-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hsb/contracts/orchestrator.py
  - tests/unit/test_orchestrator.py
  - tests/unit/test_cli.py
  - tests/integration/test_orchestrator_e2e.py
  - tests/integration/test_cli.py
  - tests/integration/test_run_loop.py
  - .claude/skills/task-orchestration/SKILL.md
autonomous: true
requirements:
  - WORC-01
  - WORC-02
  - WORC-03
  - WORC-04
  - WORC-05
  - CLIR-01
  - CLIR-02
  - CLIR-03
  - CLIR-04
  - CLIR-05
must_haves:
  truths:
    - "Pydantic WorkItemOrchInput and WorkItemOrchOutput models exist and reject extra fields"
    - "Test stub files exist for every Phase 3 requirement (Wave 0 gap closure)"
    - "task-orchestration SKILL.md exists at .claude/skills/task-orchestration/SKILL.md with disable-model-invocation: true"
    - "Every test stub has a function signature, imports, and a pytest skip marker (xfail) or pytest.fail() body until implementation lands"
  artifacts:
    - path: "src/hsb/contracts/orchestrator.py"
      provides: "WorkItemOrchInput, WorkItemOrchOutput pydantic models matching AGENT-CONTRACTS.md §3"
      contains: "class WorkItemOrchInput"
    - path: "tests/unit/test_orchestrator.py"
      provides: "Unit test stubs for WORC-02, WORC-03, WORC-04 + contract validation tests"
      contains: "def test_no_subagent_dispatch"
    - path: "tests/unit/test_cli.py"
      provides: "Unit test stubs for CLIR-02, CLIR-03, CLIR-05"
      contains: "def test_show_state_renders_table"
    - path: "tests/integration/test_orchestrator_e2e.py"
      provides: "Integration test stubs for WORC-01, WORC-05"
      contains: "pytestmark = [pytest.mark.integration]"
    - path: "tests/integration/test_cli.py"
      provides: "Integration test stub for CLIR-01"
      contains: "def test_run_next_step_triggers_lifecycle"
    - path: "tests/integration/test_run_loop.py"
      provides: "Integration test stubs for CLIR-04"
      contains: "def test_loop_terminates_when_no_ready_tasks"
    - path: ".claude/skills/task-orchestration/SKILL.md"
      provides: "Migrated task-orchestration skill with frontmatter"
      contains: "disable-model-invocation: true"
  key_links:
    - from: "src/hsb/contracts/orchestrator.py"
      to: "agents/AGENT-CONTRACTS.md §3"
      via: "Literal status enum and field names match exactly"
      pattern: "lifecycle_status.*Literal"
    - from: ".claude/skills/task-orchestration/SKILL.md"
      to: "skills/06-TASK-ORCHESTRATION.md"
      via: "body content copied verbatim with frontmatter prepended"
      pattern: "name: task-orchestration"
---

<objective>
Lay the Phase 3 foundation: pydantic contracts for the Work Item Orchestrator, Wave 0 test stubs for all 10 requirement IDs, and the task-orchestration SKILL.md migration. This plan ships no production logic — it ships verifiable scaffolding so subsequent plans (orchestrator implementation, CLI extensions, integration runs) have a contract to bind to and tests to satisfy.

Purpose: Wave 0 closes the test-file gap identified in 03-VALIDATION.md (every requirement has a `pytest` command but no test file). Contracts unblock the orchestrator wrapper code in Plan 02. SKILL.md migration follows the Phase 1/2 pattern and gives the orchestrator system prompt a canonical reference.

Output:
- `src/hsb/contracts/orchestrator.py` — pydantic models matching AGENT-CONTRACTS.md §3
- 5 test files with stub functions for WORC-01..05 and CLIR-01..05
- `.claude/skills/task-orchestration/SKILL.md` migrated from `skills/06-TASK-ORCHESTRATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-work-item-orchestrator-and-single-cycle-mvp/03-CONTEXT.md
@.planning/phases/03-work-item-orchestrator-and-single-cycle-mvp/03-RESEARCH.md
@.planning/phases/03-work-item-orchestrator-and-single-cycle-mvp/03-PATTERNS.md
@.planning/phases/03-work-item-orchestrator-and-single-cycle-mvp/03-VALIDATION.md
@agents/AGENT-CONTRACTS.md
@skills/06-TASK-ORCHESTRATION.md

<interfaces>
<!-- Phase 1/2 contract patterns the executor MUST mirror exactly. -->
<!-- Source: Phase 1 PATTERNS.md and Phase 2 contracts/qa.py — model_validator + Literal enums + extra="forbid". -->

From Phase 1/2 contracts pattern (e.g., src/hsb/contracts/qa.py):
```python
from __future__ import annotations
from typing import Literal, Optional
from pydantic import BaseModel, Field

class SomeInput(BaseModel):
    field: str
    model_config = {"extra": "forbid"}
```

From AGENT-CONTRACTS.md §3 Work Item Orchestration Contract:
- Input fields: `work_item: dict`, `epic_context: dict`, `linear_state: dict`
- Output fields: `work_item_id: str`, `lifecycle_status: Literal[...]`, `next_skill: Optional[Literal[...]]`, `handoff_payload: dict`, `lifecycle_summary: str`
- `lifecycle_status` enum: `implementation_ready | pr_ready | qa_ready | fix_required | done | escalated_to_human`
- `next_skill` enum: `implementation | git_pr_management | qa_review | linear_system_of_record`

From Phase 1 SKILL.md migration pattern (.claude/skills/linear-system-of-record/SKILL.md):
- YAML frontmatter required: `name`, `description`, `disable-model-invocation: true`, `allowed-tools`
- Body is the full content of the source skills/*.md file copied verbatim
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create WorkItemOrchInput and WorkItemOrchOutput pydantic contracts</name>
  <files>src/hsb/contracts/orchestrator.py</files>
  <read_first>
    - agents/AGENT-CONTRACTS.md (read §3 Work Item Orchestration Contract for exact field names and enum values; do NOT invent fields)
    - .planning/phases/03-work-item-orchestrator-and-single-cycle-mvp/03-PATTERNS.md (§ src/hsb/contracts/orchestrator.py — copy the Imports pattern and Full contract models verbatim)
    - src/hsb/contracts/qa.py (Phase 2 analog for model_validator + Literal status enum + extra="forbid" pattern; if it does not yet exist on disk, treat 03-PATTERNS.md excerpt as authoritative)
  </read_first>
  <action>
    Create file `src/hsb/contracts/orchestrator.py` with two pydantic models. Copy the structure verbatim from 03-PATTERNS.md § "src/hsb/contracts/orchestrator.py" Full contract models block. Required exact content:

    1. `from __future__ import annotations` import line.
    2. Imports: `from typing import Literal, Optional` and `from pydantic import BaseModel, Field`.
    3. `class WorkItemOrchInput(BaseModel)` with fields:
       - `work_item: dict`
       - `epic_context: dict`
       - `linear_state: dict`
       - `model_config = {"extra": "forbid"}`
    4. `class WorkItemOrchOutput(BaseModel)` with fields:
       - `work_item_id: str`
       - `lifecycle_status: Literal["implementation_ready", "pr_ready", "qa_ready", "fix_required", "done", "escalated_to_human"]`
       - `next_skill: Optional[Literal["implementation", "git_pr_management", "qa_review", "linear_system_of_record"]] = None`
       - `handoff_payload: dict = Field(default_factory=dict)`
       - `lifecycle_summary: str`  # WORC-05 — posted to Linear as a comment after each cycle
       - `model_config = {"extra": "forbid"}`
    5. Add docstrings on both classes referencing AGENT-CONTRACTS.md §3.

    Do NOT add a model_validator — Phase 3 contract is a pure schema (cycle-cap enforcement lives in Phase 2 QAOutput model_validator and the Layer 2 orchestrator safety net in Plan 02). Do NOT add fields beyond AGENT-CONTRACTS.md §3.
  </action>
  <verify>
    <automated>cd /home/ubuntu/hugo/task-management-agents &amp;&amp; python -c "from hsb.contracts.orchestrator import WorkItemOrchInput, WorkItemOrchOutput; o = WorkItemOrchOutput(work_item_id='LIN-1', lifecycle_status='done', handoff_payload={}, lifecycle_summary='ok'); print(o.model_dump_json())"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f src/hsb/contracts/orchestrator.py`
    - Contains exact class declarations: `grep -q "class WorkItemOrchInput(BaseModel)" src/hsb/contracts/orchestrator.py`
    - Contains exact class declarations: `grep -q "class WorkItemOrchOutput(BaseModel)" src/hsb/contracts/orchestrator.py`
    - Contains all 6 lifecycle_status enum values: `grep -q "implementation_ready" src/hsb/contracts/orchestrator.py && grep -q "escalated_to_human" src/hsb/contracts/orchestrator.py && grep -q "fix_required" src/hsb/contracts/orchestrator.py`
    - Both models forbid extras: `grep -c '"extra": "forbid"' src/hsb/contracts/orchestrator.py` returns `2`
    - lifecycle_summary field present: `grep -q "lifecycle_summary: str" src/hsb/contracts/orchestrator.py`
    - No model_validator decorator: `! grep -q "model_validator" src/hsb/contracts/orchestrator.py`
  </acceptance_criteria>
  <done>
    Both pydantic models load without import error, validate a sample WorkItemOrchOutput dict, and reject any extra field with ValidationError. Field set matches AGENT-CONTRACTS.md §3 exactly — no additions, no omissions.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Wave 0 test stubs for all 10 requirement IDs</name>
  <files>tests/unit/test_orchestrator.py, tests/unit/test_cli.py, tests/integration/test_orchestrator_e2e.py, tests/integration/test_cli.py, tests/integration/test_run_loop.py</files>
  <read_first>
    - .planning/phases/03-work-item-orchestrator-and-single-cycle-mvp/03-PATTERNS.md (§ tests/unit/test_orchestrator.py, § tests/unit/test_cli.py, § tests/integration/test_orchestrator_e2e.py, § tests/integration/test_cli.py, § tests/integration/test_run_loop.py — copy each test function signature verbatim)
    - .planning/phases/03-work-item-orchestrator-and-single-cycle-mvp/03-VALIDATION.md (Per-Task Verification Map — every test function name in the "Automated Command" column MUST exist as a stub)
    - src/hsb/contracts/orchestrator.py (just created in Task 1; tests import from it)
  </read_first>
  <action>
    Create five test files, each containing the exact function names from 03-VALIDATION.md. Every stub function MUST contain `pytest.fail("Wave 0 stub — implemented in Plan 02/03/04")` so the full suite is RED until implementation lands (Nyquist enforcement). Imports must compile (use try/except ImportError around imports of modules that do not exist yet, or wrap with `pytest.importorskip("hsb.agents.work_item_orchestrator")` at module level).

    File 1: `tests/unit/test_orchestrator.py` — copy the stub signatures from 03-PATTERNS.md § tests/unit/test_orchestrator.py verbatim. Required test function names (each MUST be a stub that fails):
    - `test_no_subagent_dispatch_in_options` (WORC-02)
    - `test_qa_cycle_cap_model_validator` (WORC-03 Layer 1)
    - `test_qa_cycle_cap_safety_net_posts_comment` (WORC-03 Layer 2 — async, mark with `@pytest.mark.asyncio`)
    - `test_tool_wrapper_requires_full_issue_content` (WORC-04)
    - `test_valid_orch_output_passes` (contract validation — implement now, NOT a stub; uses WorkItemOrchOutput from Task 1)
    - `test_invalid_lifecycle_status_fails` (contract validation — implement now)
    - `test_orch_output_extra_field_rejected` (contract validation — implement now)
    Add `import pytest`, `from pydantic import ValidationError`, `from hsb.contracts.orchestrator import WorkItemOrchInput, WorkItemOrchOutput`. The three contract validation tests MUST be functional now (they only depend on Task 1 contracts). The four behavior tests MUST be stubs with `pytest.fail()`.

    File 2: `tests/unit/test_cli.py` — copy stub signatures from 03-PATTERNS.md § tests/unit/test_cli.py. Required test function names (each MUST be a stub that fails):
    - `test_show_state_renders_table` (CLIR-02)
    - `test_show_next_action_no_side_effects` (CLIR-03)
    - `test_run_next_step_uses_asyncio_run` (CLIR-05)
    - `test_show_state_uses_asyncio_run` (CLIR-05)
    - `test_no_process_state` (CLIR-05 — verifies no global state is set in module)
    Add imports: `import pytest`, `from typer.testing import CliRunner`. Wrap `from hsb.cli.main import app` in try/except ImportError -> module-level `pytestmark = pytest.mark.skip(reason="Wave 0 stub: hsb.cli.main not yet extended")` so collection does not fail.

    File 3: `tests/integration/test_orchestrator_e2e.py` — copy stub signatures from 03-PATTERNS.md § tests/integration/test_orchestrator_e2e.py. Required: `pytestmark = [pytest.mark.integration]`. Required test function names (each MUST be a stub that fails):
    - `test_full_lifecycle_todo_to_done` (WORC-01)
    - `test_lifecycle_comment_persisted` (WORC-05)
    - `test_qa_cycle_cap_not_exceeded` (WORC-03)
    - Also include alias tests `test_full_lifecycle` and `test_lifecycle_comment` matching VALIDATION.md command IDs (these may simply call the canonical test or duplicate the stub).
    All marked `@pytest.mark.asyncio` where async.

    File 4: `tests/integration/test_cli.py` — copy stub signatures from 03-PATTERNS.md § tests/integration/test_cli.py. Required: `pytestmark = [pytest.mark.integration]`. Required test function:
    - `test_run_next_step_triggers_lifecycle` (CLIR-01) — also include alias `test_run_next_step` matching VALIDATION.md.
    - `test_show_state_returns_table_output` (also CLIR-02 integration variant)
    Stub bodies fail with `pytest.fail("Wave 0 stub")`.

    File 5: `tests/integration/test_run_loop.py` — copy stub signatures from 03-PATTERNS.md § tests/integration/test_run_loop.py. Required: `pytestmark = [pytest.mark.integration]`. Required test function names:
    - `test_loop_terminates_when_no_ready_tasks` (CLIR-04 — also alias `test_loop_terminates`)
    - `test_loop_exits_on_ctrl_c` (CLIR-04)
    - `test_loop_stops_on_run_next_step_failure` (Pitfall 5 protection)
    Stub bodies fail with `pytest.fail("Wave 0 stub")`.

    For every stub-failing test, the failure message MUST literally contain the string `"Wave 0 stub"` so a grep can confirm completeness.
  </action>
  <verify>
    <automated>cd /home/ubuntu/hugo/task-management-agents &amp;&amp; python -m pytest tests/unit/test_orchestrator.py::test_valid_orch_output_passes tests/unit/test_orchestrator.py::test_invalid_lifecycle_status_fails tests/unit/test_orchestrator.py::test_orch_output_extra_field_rejected -v</automated>
  </verify>
  <acceptance_criteria>
    - All 5 test files exist: `for f in tests/unit/test_orchestrator.py tests/unit/test_cli.py tests/integration/test_orchestrator_e2e.py tests/integration/test_cli.py tests/integration/test_run_loop.py; do test -f "$f"; done`
    - Every required test function from VALIDATION.md is present: `grep -l "test_no_subagent_dispatch" tests/unit/test_orchestrator.py && grep -l "test_qa_cycle_cap" tests/unit/test_orchestrator.py && grep -l "test_full_context_in_tool_calls\|test_tool_wrapper_requires_full_issue_content" tests/unit/test_orchestrator.py && grep -l "test_show_state_renders" tests/unit/test_cli.py && grep -l "test_show_next_action_no_side_effects" tests/unit/test_cli.py && grep -l "test_no_process_state\|test_run_next_step_uses_asyncio_run" tests/unit/test_cli.py && grep -l "test_full_lifecycle" tests/integration/test_orchestrator_e2e.py && grep -l "test_lifecycle_comment" tests/integration/test_orchestrator_e2e.py && grep -l "test_run_next_step" tests/integration/test_cli.py && grep -l "test_loop_terminates" tests/integration/test_run_loop.py`
    - Every stub function fails with the marker string: `grep -rn "Wave 0 stub" tests/` returns at least 11 matches (one per stub function across all 5 files; contract validation tests in test_orchestrator.py do NOT count)
    - Integration tests have the marker: `grep -l "pytest.mark.integration" tests/integration/test_orchestrator_e2e.py tests/integration/test_cli.py tests/integration/test_run_loop.py | wc -l` returns `3`
    - Three contract validation tests pass: `pytest tests/unit/test_orchestrator.py::test_valid_orch_output_passes tests/unit/test_orchestrator.py::test_invalid_lifecycle_status_fails tests/unit/test_orchestrator.py::test_orch_output_extra_field_rejected -v` exits 0
    - `pytest tests/unit/ --collect-only` succeeds (no import errors at collection time)
  </acceptance_criteria>
  <done>
    Five test files exist with every required test function name from 03-VALIDATION.md. Three contract validation tests pass. All other tests are stubs that fail with "Wave 0 stub" message. Test collection works on the unit suite. Integration files are marked with `pytest.mark.integration`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate task-orchestration SKILL.md</name>
  <files>.claude/skills/task-orchestration/SKILL.md</files>
  <read_first>
    - skills/06-TASK-ORCHESTRATION.md (full body content; copy verbatim into the new file body)
    - .planning/phases/03-work-item-orchestrator-and-single-cycle-mvp/03-PATTERNS.md (§ .claude/skills/task-orchestration/SKILL.md — copy the YAML frontmatter exactly)
    - .planning/phases/03-work-item-orchestrator-and-single-cycle-mvp/03-RESEARCH.md (Pattern 5 — confirms `disable-model-invocation: true` is mandatory for skills with Linear write side effects)
    - .claude/skills/linear-system-of-record/SKILL.md (Phase 1 analog — confirms frontmatter pattern is consistent across phases; if not yet on disk, the 03-PATTERNS.md excerpt is authoritative)
  </read_first>
  <action>
    Create directory `.claude/skills/task-orchestration/` and write the file `SKILL.md`. Structure:

    1. Open with YAML frontmatter (between `---` lines). Use this exact frontmatter:
       ```yaml
       ---
       name: task-orchestration
       description: |
         Work Item Orchestrator — drives a single task from todo to done through the full lifecycle
         (Linear read → Builder → Git → QA → fix loop → done).
         Only invoke when: operator triggers run-next-step on a specific work item.
         Side effects: updates Linear state, calls Builder/Git/QA agents.
         Do NOT invoke during conversation or without an explicit work item trigger.
       disable-model-invocation: true
       allowed-tools: "mcp__agents__run_builder mcp__agents__run_git mcp__agents__run_qa mcp__agents__run_linear_op mcp__linear__get_issue mcp__linear__update_issue mcp__linear__create_comment mcp__linear__list_issues"
       arguments:
         - name: work_item_id
           description: "Linear work item ID to orchestrate (e.g. LIN-42)"
       ---
       ```
    2. After the closing `---`, append the COMPLETE body of `skills/06-TASK-ORCHESTRATION.md` verbatim. Do NOT modify, summarize, or paraphrase the body — copy line for line. The original file at `skills/06-TASK-ORCHESTRATION.md` MUST remain untouched (it is the human-readable reference per the Phase 1 SKILL.md migration pattern, D-07).

    Do NOT add any other files in `.claude/skills/task-orchestration/` (no README, no auxiliary files).
  </action>
  <verify>
    <automated>cd /home/ubuntu/hugo/task-management-agents &amp;&amp; test -f .claude/skills/task-orchestration/SKILL.md &amp;&amp; head -1 .claude/skills/task-orchestration/SKILL.md | grep -q '^---$' &amp;&amp; grep -q "name: task-orchestration" .claude/skills/task-orchestration/SKILL.md &amp;&amp; grep -q "disable-model-invocation: true" .claude/skills/task-orchestration/SKILL.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .claude/skills/task-orchestration/SKILL.md`
    - Original source still present: `test -f skills/06-TASK-ORCHESTRATION.md`
    - Frontmatter opens on line 1: `head -1 .claude/skills/task-orchestration/SKILL.md` equals `---`
    - Frontmatter contains required fields: `grep -q "^name: task-orchestration$" .claude/skills/task-orchestration/SKILL.md && grep -q "^disable-model-invocation: true$" .claude/skills/task-orchestration/SKILL.md && grep -q "allowed-tools" .claude/skills/task-orchestration/SKILL.md`
    - allowed-tools includes the four mcp__agents__ tools: `grep -q "mcp__agents__run_builder" .claude/skills/task-orchestration/SKILL.md && grep -q "mcp__agents__run_git" .claude/skills/task-orchestration/SKILL.md && grep -q "mcp__agents__run_qa" .claude/skills/task-orchestration/SKILL.md && grep -q "mcp__agents__run_linear_op" .claude/skills/task-orchestration/SKILL.md`
    - Body is at least 80% as long as the source skill body: `[ $(wc -l < .claude/skills/task-orchestration/SKILL.md) -ge $(wc -l < skills/06-TASK-ORCHESTRATION.md) ]` (frontmatter adds lines, so new file MUST be >= source line count)
    - The closing `---` of frontmatter exists: `awk '/^---$/{c++} END{exit (c<2)}' .claude/skills/task-orchestration/SKILL.md` exits 0
  </acceptance_criteria>
  <done>
    The migrated SKILL.md exists with correct YAML frontmatter (name, description, disable-model-invocation: true, allowed-tools, arguments) and the full unmodified body of skills/06-TASK-ORCHESTRATION.md after the frontmatter. The original source file is unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Linear issue content → orchestrator system prompt | Untrusted content authored by humans flows into the LLM context; mitigated downstream in Plan 02 by passing as structured JSON in tool args, not raw string in system_prompt |
| Test stub imports → CI runner | Test files import not-yet-existing modules; mitigated by try/except ImportError + pytest skip markers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | Pydantic contracts (orchestrator.py) | mitigate | `model_config = {"extra": "forbid"}` rejects unexpected fields; `Literal` enums on `lifecycle_status` and `next_skill` reject unknown values; this prevents prompt-injection payloads in Linear issue content from reaching downstream tool calls in unexpected schema shapes |
| T-3-02 | Information Disclosure | Test stubs | accept | Test stub bodies do not log credentials; no API keys referenced in any stub. Risk: low (test scaffolding only) |
| T-3-04 | Elevation of Privilege | Contract enum values | mitigate | `lifecycle_status` Literal includes `escalated_to_human` as the explicit terminal state for QA cycle cap escalation (D-05 Layer 2); no value path bypasses the cap |
</threat_model>

<verification>
- All three contract validation tests pass: `pytest tests/unit/test_orchestrator.py -k "valid_orch_output or invalid_lifecycle or extra_field" -v` exits 0
- Wave 0 marker count: `grep -rn "Wave 0 stub" tests/ | wc -l` is at least 11
- Pydantic models import successfully: `python -c "from hsb.contracts.orchestrator import WorkItemOrchInput, WorkItemOrchOutput"` exits 0
- SKILL.md has valid frontmatter: `python -c "import yaml; doc = open('.claude/skills/task-orchestration/SKILL.md').read().split('---')[1]; print(yaml.safe_load(doc))"` exits 0 and prints a dict with `name`, `disable-model-invocation`, `allowed-tools`
</verification>

<success_criteria>
- [ ] `src/hsb/contracts/orchestrator.py` exists with WorkItemOrchInput + WorkItemOrchOutput, both `extra="forbid"`, all 6 lifecycle_status enum values, all 4 next_skill enum values, lifecycle_summary field
- [ ] 5 test files exist with every requirement-mapped test function from 03-VALIDATION.md present as either implemented (3 contract tests) or stubbed with `pytest.fail("Wave 0 stub …")`
- [ ] 3 contract validation tests in tests/unit/test_orchestrator.py pass
- [ ] Test collection succeeds on the unit suite
- [ ] `.claude/skills/task-orchestration/SKILL.md` exists with `disable-model-invocation: true` and the full body of skills/06-TASK-ORCHESTRATION.md verbatim
- [ ] `skills/06-TASK-ORCHESTRATION.md` is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-work-item-orchestrator-and-single-cycle-mvp/03-01-SUMMARY.md` summarizing:
- Files created (6 paths)
- Wave 0 stub count (target: 11+ stubs across 5 test files)
- 3 contract validation tests passing
- Confirmation that skills/06-TASK-ORCHESTRATION.md is untouched (sha matches pre-task value)
</output>
